@model Exception

@(Smc.Message(SMCMessageType.Error, "erroAluno", "Ocorreu um erro", Model.Message)
    .Button("Deslogar", "Logout", "Home", new { area = "" }, x => x.Type(SMCButtonType.Link))
    .HideCloseButton()
    .AutoShow())