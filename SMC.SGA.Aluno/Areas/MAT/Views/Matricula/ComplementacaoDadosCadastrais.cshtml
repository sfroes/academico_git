@model ComplementacaoDadosCadastraisViewModel

@section MenuLateral {
    @Html.Action("MenuPaginas", Model)
}

@(Smc.SGFTemplate().Secao().Token(MatriculaTokens.PAGINA_COMPLEMENTACAO_DADOS_CADASTRAIS_INSTRUCOES))

@using (Smc.Form("frmPagina").Ajax().Begin())
{
    @Html.Partial("_BotoesNavegacao", Model)
}