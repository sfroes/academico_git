@model ComponenteCurricularDetalheDivisoesViewModel

@using (Smc.Div("divItem").Size(SMCSize.Grid24_24).CssClass("").Begin())
{
    using (Smc.Div("divItem").CssClass("smc-listdetailed-conteudo").Size(SMCSize.Grid24_24).Begin())
    {
        using (Smc.Div("divItem").CssClass("").Size(SMCSize.Grid24_24).Begin())
        {
            @Smc.DisplayFor(m => m.DescricaoCompleta).HideLabel().CssClass("smc-listdetailed-conteudo-subtitulo-destaque").Size(SMCSize.Grid12_24)
            @Smc.DisplayFor(m => m.CargaHorariaGradeFormatada).HideLabel().CssClass("smc-listdetailed-conteudo-subtitulo-destaque")
        }
    }
}