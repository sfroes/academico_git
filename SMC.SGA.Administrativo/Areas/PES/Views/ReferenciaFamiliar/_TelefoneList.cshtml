@using SMC.SGA.Administrativo.Models
@model TelefoneViewModel

@using (Smc.Div().CssClass("smc-listdetailed-detalhes-conteudo-item").Size(SMCSize.Grid24_24).Begin())
{
    @Smc.DisplayFor(m => m.TelefoneFormatado).HideLabel().Size(SMCSize.Grid22_24);
}